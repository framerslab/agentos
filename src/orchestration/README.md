# orchestration/

Turn loop and routing. The four-stage Cognitive Pipeline (`pipeline/{ingest,memory,query,read}/`), workflows, planner, HITL approvals, agent definitions and multi-agent agency. Whatever decides what happens next on a turn lives here.
