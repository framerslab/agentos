# core/

Foundational primitives only. LLM clients, embeddings, vector store, storage, streaming, conversation, logging, rate-limiting, tools (`ITool`), provider config, types. Anything domain-specific (memory, RAG, channels, guardrails) lives elsewhere.
